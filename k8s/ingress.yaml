apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dummy-app-ingress
  labels:
    app: dummy-app
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: dummy-app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dummy-app-service
            port:
              number: 80
